var censorwords:{"sad","bad","mad"};
var custom censoredwords=[];
function censor(instr){
    for(idx in censored words){
    instr=instr.replace(censored words[idx]);
}
return instr;
}
function addcensoredword(word){
    custom censoredwords.push(word);
}
function getcensoredwords{y}
return censoredword.cocat(customcensoredwords);


exports.cens or=censor;
exports.addcensoredword=addcensoredword;
exports.censoredwords=getcensoredwords;

